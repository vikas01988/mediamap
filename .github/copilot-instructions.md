# Cinewave workspace

- Use JavaScript ES modules.
- Keep booking state transitions in server services.
- Preserve atomic seat-locking semantics and validate ownership before mutation.
- Run `npm run build` after client changes and exercise API routes with a configured MongoDB instance.
